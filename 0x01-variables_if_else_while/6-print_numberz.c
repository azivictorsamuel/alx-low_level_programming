#include <stdio.h>

/**
   * main - prints alphabet in reverse
   *
   * Return: zero
   */
int main(void)
{
	int i;

	for (i = '0'; 0 <= '10'; 1++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
